version: '3'
services:
  db:
    image: postgres:11
  web:
    restart: always
    build: .
    environment:
      - ICMS_WEB_PORT=8080
      - ICMS_DEBUG=True
    tty: true
    ports:
      - "8080:8080"
    volumes:
      - .:/code
    depends_on:
      - db
